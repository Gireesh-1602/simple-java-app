pipeline {
    agent {
        // Use the Node.js 16 Docker image
        docker {
            image 'node:16'
            
        }
    }

    stages {
        stage('Install Dependencies') {
            steps {
                // Install Node.js dependencies
                sh 'node --version'
            }
        }

    }
}

